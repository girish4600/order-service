spring:
  application:
    name: order-service
#  kafka:
#    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS}
#    #added to auto crate topics in kafka
#    admin:
#      auto-create: true

  cloud:
    gcp:
      project-id: one-step-gcp

server:
  port: 8080

#enable this when we used eureka service
#eureka:
#  client:
#    service-url:
#      defaultZone: http://service-registry:8761/eureka/
#
#  instance:
#    prefer-ip-address: true
#    non-secure-port: 8082
